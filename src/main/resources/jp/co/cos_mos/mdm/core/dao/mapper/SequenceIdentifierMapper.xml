<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="jp.co.cos_mos.mdm.core.dao.mapper.SequenceIdentifierMapper">

<select id="select" parameterType="String" resultType="SequenceIdentifier">
	SELECT
	    class_name AS className,
	    sequence_id AS sequenceId,
	    inactive_ts AS inactiveTs,
	    last_update_ts AS lastUpdateTs,
	    last_update_user AS lastUpdateUser,
	    last_update_tx_id AS lastUpdateTxId
	FROM
	    sequence_identifier
	WHERE
	        class_name = #{className}
</select>

</mapper>